pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "NexLinkSocial"

// ── NexLink Social (docs/social/10-modules.md §10.3) ──────────────────────
// Split out of THVjQ/NexLink on 2026-09-21. :app, :shared and :wear stayed
// behind in that repository.
//
// §10.4's dependency rule said ":app may depend on :social-contract and
// NOTHING else from this list". It never did depend on it — the boundary is
// now a repository boundary, which is the strongest form the rule has taken.
// The Gradle tripwire in build.gradle is kept for the day someone re-adds
// :app here.
include ':rustls-tls'          // VENDORED — the SDK's TLS verifier. See its VENDORED.md.
include ':social-contract'     // IPC types shared with :app. No SDK, no network.
include ':social-core'         // Matrix session, store, crypto, sync
include ':social-ui'           // chat, onboarding, settings screens
include ':social-rtc'          // calls — §17, §18, §19. Added for phase 4 (§33.5).
include ':social'              // com.thvjq.nexlink.social — the application
